name = "defol_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
defol = "*"

[[pins]]
defol = { path = ".." }
den_tests = { path = "../../den/tests" }

[build-profiles]
defol       = "validation"
defol_tests = "validation"
