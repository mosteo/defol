#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	gprbuild -P debian/gpr/defol_builder.gpr -XDEFOL_BUILD_PROFILE=release

override_dh_auto_clean:
	gprclean -P debian/gpr/defol_builder.gpr -XDEFOL_BUILD_PROFILE=release || true
	dh_auto_clean

override_dh_auto_install:
